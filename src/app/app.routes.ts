import { Routes } from '@angular/router';
import { PrincipalComponent } from './main/principal/principal.component';

export const routes: Routes = [
    {path: 'prueba', component: PrincipalComponent},
];